<%= render partial: "ajion2596dhi/shared/head" %>

<div class="container">
  <div class="row">
    <%= link_to "Back", ajion2596dhi_root_path %>
    <br>
    name: <%= @item.name %><br>
    price: <%= @item.price %><br>
    <%= image_tag @item.main_image, width: "150px" %>
    
    
    <br>
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>#</th>
          <th>image</th>
          <th>color</th>
          <th>size</th>
          <th>#</th>
        </tr>
      </thead>

      <tbody>
        <% @item.products.each do |item| %>
          <tr>
            <th scope="row"><%= item.id %></th>
            <td><%= image_tag item.image, width: "30px" %></td>
            <td>
              <div class="colorbox" style="float: left; background-color: #<%= item.color %>;"></div>
            </td>
            <td><%= item.size %>
            <td>
              <%= link_to "edit", edit_detail_ajion2596dhi_product_path(item)  %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <


  </div>
</div>